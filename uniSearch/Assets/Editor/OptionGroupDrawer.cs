using UnityEngine;
using UnityEditor;
using System.Collections;

/*
[CustomPropertyDrawer(typeof(OptionGroup))]
public class OptionGroupDrawer : PropertyDrawer  {

	public override void OnGUI (Rect position, SerializedProperty property, GUIContent label)
	{
		base.OnGUI (position, property, label);
		Debug.Log("Title:" + property.FindPropertyRelative("Title").ToString());
		Debug.Log("TestField:" + property.FindPropertyRelative("TestField").ToString());
	}
}
*/
